apiVersion: v1
kind: Service
metadata:
  name: {{ include "getuser.fullname" . }}
  labels:
    {{- include "getuser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "getuser.selectorLabels" . | nindent 4 }}
